<!doctype html>
<html>
    <head>
        {% include "head.njk" %}
    </head>
    <body>
        <h1>Torcellini Stuff</h1>
        <main>
            <p>This website is still under construction, but perhaps one of these other sites will be interesting!</p>
            <section id="websites"><ul>
                {% for site in websites %}
                <li><article>
                    <h2><a href="{{site.url}}">{{site.name}}</a></h2>
                    <p>{{ site.description }}</p>
                    {% if site.feed %}
                        {% feed site.feed, 3 %}
                    {% endif %}
                </ul></article>
                {% endfor %}
            </section>
        </main>
    </body>
</html>
                